// import kaboom from "https://unpkg.com/kaboom@next/dist/kaboom.mjs";

export default kaboom({
  noGlobal: true,
  scale: 2,
  debug: true,
  width: 500,
  height: 300,
  clearColor: [0, 0, 0, 1],
});
